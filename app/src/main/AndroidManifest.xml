<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrador.curso4miscontactos">
    
    <uses-permission android:name="android.permission.CALL_PHONE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.administrador.curso4miscontactos.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.administrador.curso4miscontactos.DetalleContacto"
            android:parentActivityName="com.example.administrador.curso4miscontactos.MainActivity">
            <!-- Soporte el boton de subir o regreso para versiones 4.0 o anteriores -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.administrador.curso4miscontactos.MainActivity" >
            </meta-data>
        </activity>
    </application>

</manifest>